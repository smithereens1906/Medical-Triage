[project]
name = "medical-triage"
version = "0.1.0"
description = "RL-based Medical Triage System"
requires-python = ">=3.10"

dependencies = [
    "fastapi",
    "uvicorn",
    "pydantic",
    "numpy",
    "pyyaml"
]